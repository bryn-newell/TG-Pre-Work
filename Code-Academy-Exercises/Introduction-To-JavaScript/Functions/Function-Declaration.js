function getReminder() {
  console.log('Water the plants.')
}

function greetInSpanish() {
  console.log('Buenas Tardes.')
}

// A function declaration binds a function to a name or identifier. A declaration consists of the keyword
// function the identifier followed by a set of parentheses and the function body enclosed by {}. The
// hoisting feature in JS also allows us to reference a function before it has been defined.
