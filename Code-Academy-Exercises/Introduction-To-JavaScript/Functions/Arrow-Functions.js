const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};


// Arrow function syntax is a new and shorter way to write functions using the fat arrow () => notation.
// It removes the need to type out function each time a function is definited. The parameters go inside the ()
// and the arrow points to the body of the function which is still wrapped in {}.
