function makeShoppingList(item1 ='milk', item2='bread', item3='eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}

// Default parameters allow parameters to have a default value in case no argument is passed into the function
// or if the argument is undefined. The = is used to pass the default value to the parameter inside the function
// declaration.
