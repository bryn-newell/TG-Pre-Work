function monitorCount(rows,columns) {
  return rows*columns;
}

const numOfMonitors = monitorCount(5,4);
console.log(numOfMonitors);

// When a function is called the computer will run the function's code and evaluate the result of the code.
// By default this value is undefined. The return statement lets us pass back information from the function.
// It is created by using the return keyword followed by the return value we would like to know. When a
// return statement is used in a function body the execution of the function is stopped and the following
// code is not executed. 
