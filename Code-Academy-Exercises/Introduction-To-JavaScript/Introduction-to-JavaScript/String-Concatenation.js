console.log('Hello' + ' '+ 'World');

// When a + operator is used on strings it appends the right string to the left string, this is called
// concatenation. Don't forget to add a space at the end or beginning of a string when concatenating if
// necessary!
