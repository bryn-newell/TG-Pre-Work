console.log('Teaching the world how to code'.length);

// Every string has a property called length that stores the number of characters in that string. Property
// information can be accessed by appending a period and the property name such as .length. The . is called
// the dot operator.
