let Airplane = {};
Airplane.myAirplane = "StarJet";
module.exports = Airplane;

// A module is defined with our let or const keyword followed by a Capitalized name = {} . Then
// ModuleName.property is then used to add data to the object by setting properties and giving them values.
// module.exports = ModuleName exports the object as a module. So to export a module we define an object to
// represent the module, then add data or behavior to the module, then export the module.
