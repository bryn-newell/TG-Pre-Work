const entree = 'Enchiladas';
console.log(entree);
const testing;

// The value of a variable defined with const cannot be redefined. Const values also must be declared with
// a value otherwise a syntax error will be returned and if you try to reassign the value a type error will
// occur. 
