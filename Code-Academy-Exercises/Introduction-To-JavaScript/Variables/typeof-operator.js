let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1

console.log(typeof newVariable);

// The typeof operator can be used the check the data type of a variable's value. typeof returns a string
// describing the data type. 
