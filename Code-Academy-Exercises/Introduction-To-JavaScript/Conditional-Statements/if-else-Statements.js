let sale = true;

sale = false;
d
if(sale) {
  console.log('Time to buy!');
} else { console.log('Time to wait for a sale.')}

// An else statement must be paired with an if statement and includes a code block to run if the condition
// inside () is false. if else statments allow us to automate solutions to binary decisions (yes or no).
