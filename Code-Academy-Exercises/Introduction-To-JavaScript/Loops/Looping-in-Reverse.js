// The loop below loops from 0 to 3. Edit it to loop backwards from 3 to 0
for (let counter = 3; counter >=0 ; counter--){
  console.log(counter)
};

// To loop backwards the iterator variable must be set to the highest desired value and the iterator should
// decrease in intervals after each loop using --.  
