// Write your code below
const vacationSpots = ['Greece','Asia', 'Hawaii'];
console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

// Loops make it much easier on us so that we don't have to rewrite the same code over and over again. 
