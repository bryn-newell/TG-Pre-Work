const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below
for ( let i = 0; i < vacationSpots.length; i++ ){
  console.log('I would love to visit ' + vacationSpots[i])
}

// Loops are very helpful to iterate through data structures such as arrays. To loop through each element
// in an array the .length property should be used in its condition. i is often used as the name for the
// iteration variable, short for index. 
