class Surgeon {
  constructor(name, department) {
    this.name = name;
    this.department = department;
  }
}

/* The constructor() method is called every time it creates a new instance of a class. By convention
we capitalize and CamelCase class names. The constructor() method accepts arguments. Inside the constructor
method we use the this keyword, to refer to an instance of that class.
