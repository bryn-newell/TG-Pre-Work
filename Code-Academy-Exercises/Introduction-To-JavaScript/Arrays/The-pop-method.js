const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];

chores.pop();
console.log(chores);

// The .pop() method removes the last item of an array. It does not take any arguments because it only takes
// off the last item. It also returns the value of the last element and mutates the original array like push.
