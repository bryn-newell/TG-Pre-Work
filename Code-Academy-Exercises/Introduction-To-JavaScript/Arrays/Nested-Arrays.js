const numberClusters = [[1,2], [3,4], [5,6]];
const target = numberClusters[2][1];

// A nested array is when an array contains another array. To access the nested array bracket notation with
// the index value can be used in the same way. To access elements inside the nested array we can chain bracket
// notation with more index values such as [1][0].
