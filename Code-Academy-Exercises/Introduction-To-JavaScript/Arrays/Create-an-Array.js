const hobbies = ['skiing', 'climbing', 'watching movies']
console.log(hobbies)

// An array literal creates an array by wrapping items in square brackets []. Each content item inside an array
// is called an element, separated by commas. An array can also be saved to a variable.
