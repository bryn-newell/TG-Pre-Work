const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('clean room', 'sweep floor');
console.log(chores);

// The .push() method allows us to add items to the end of an array. It is a built in function, that is called
// with dot notation and it can take a single or multiple arguments separated by commas. It can also be referred
// to as a destructive array method because it changes the original array. 
